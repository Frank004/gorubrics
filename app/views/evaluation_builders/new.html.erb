<br>
<div class="col-md-5 col-md-offset-3 col-lg-offset-3">
  <h1>Nueva Evaluación</h1>
  <div class="authform well">
	<%= form_for([@event,@evaluation_builder], :html => { :class => 'form survey-form clearfix' }) do |f| %>
	 <%= render 'form', f: f %>
	  <div class="actions">
	    <%= f.submit "Crear",  class: 'btn btn-warning' %>
    	<%= link_to 'regresar', event_evaluation_builders_path(@event),  class: 'btn btn-primary'  %>
	  </div>
	<% end %>
  </div>
</div>