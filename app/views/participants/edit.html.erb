<br>
<div class="col-md-5 col-md-offset-3 col-lg-offset-3">
  <h1>Editar Registro</h1>
  <div class="authform well">
	<%= form_for(@participant, :html => { :class => 'form survey-form clearfix' }) do |f| %>
	 <%= render 'form', f: f %>
	  <div class="actions">
	    <%= f.submit "Actualizar",  class: 'btn btn-warning' %>
    	<%= link_to 'regresar', event_participants_path(@event),  class: 'btn btn-primary'  %>
	  </div>
	<% end %>
  </div>
</div>