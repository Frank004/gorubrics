<p id="notice"><%= notice %></p>

<h1>Listing Criterions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Evaluation builder</th>
      <th>Organization</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @criterions.each do |criterion| %>
      <tr>
        <td><%= criterion.name %></td>
        <td><%= criterion.evaluation_builder %></td>
        <td><%= criterion.organization %></td>
        <td><%= link_to 'Show', criterion %></td>
        <td><%= link_to 'Edit', edit_criterion_path(criterion) %></td>
        <td><%= link_to 'Destroy', criterion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Criterion', new_criterion_path %>
