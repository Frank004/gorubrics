<%= form_for(@criterion) do |f| %>
  <% if @criterion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@criterion.errors.count, "error") %> prohibited this criterion from being saved:</h2>

      <ul>
      <% @criterion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :evaluation_builder_id %><br>
    <%= f.text_field :evaluation_builder_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.text_field :organization_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
